# Preserved, not deleted

Stopped on 2026-09-18 at the user's request to preserve useful work and restart
from a sound checkpoint. Only this replay's runner (PID 68969) was terminated.
Ollama and unrelated app processes were left running.

The exact evidence input, ten completed discovery calls across this run and its
parent, raw model responses, provisional 149-node/1,175-edge board, and partial
verification are retained. The board and partial verification are NOT accepted
facts and are NOT being reused as authority.

Restart: `../qwen-v2-consolidated-2c95T4/`.
It reuses the ten discovery responses as draft candidates and recomputes
consolidation, linking, branch accountability and verification. It does not rerun
the storyteller or upstream investigation, add evidence, or overwrite an app version.

Pre-change code/prompts/scripts backup:
`../preserved-before-4.3.6-3NEIXX/`.
