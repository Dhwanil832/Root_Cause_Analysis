# Literal-claim v5 acceptance — frozen before live calls

Scope: repair claim review, not certify the complete RCA system. Same Qwen digest and provider settings as the paused application run. No storyteller, fallback, hidden answer injection, edited model verdicts or application writes. One complete original assertion is reviewed in one model call against original sources. The meaning-tree stage is removed.

## Unchanged regressions (18 cases)

Use the exact saved five producer findings, seven `claim-parts-fresh-inputs.json` cases, and six `fixed-meaning-heldout-inputs.json` cases. These are REGRESSIONS, not new held-out tests. All evidence, propositions and semantic expectations in `fixed-meaning-expectations.md` remain unchanged. The obsolete requirement for two calls is replaced by one literal-claim review; do not generate fragments.

Saved statuses: partial/unknown, partial/unknown, supported, supported record context, supported record context. None require a source conflict.

Seven statuses: supported, unknown, unknown, contradicted, contradicted, supported record context, unknown with two-sided source conflict. No other conflicts expected.

Six statuses: partial (unknown only if inseparable), supported, unknown with both accounts, contradicted, contradicted, supported record context with underlying-reality conflict. Do not confuse a report's content with the truth of the described event.

## New transfer cases (six)

Authored and frozen before any live call in this evaluation. This is a small developer-authored transfer set, not an independent benchmark.

| ID | Expected status | Conflict | Required distinction |
|---|---|---|---|
| literal-transfer-01 | unknown | none | Chronology and pressure decline do not isolate a seal's causal contribution; no fragment-level partial credit. |
| literal-transfer-02 | contradicted | none | Archive contains an expired certificate, but the work ticket identifies the valid replacement. Cite both validity and ticket. |
| literal-transfer-03 | contradicted | none | Different times and heater states are not conflicting readings of the same state. |
| literal-transfer-04 | supported | none | Complete contemporaneous physical observation supports this scoped absence. |
| literal-transfer-05 | supported record context | open, both sides | Register content is known; physical weight is disputed. |
| literal-transfer-06 | contradicted | none | An AND requirement is not satisfied by one condition; a later test does not satisfy before-restart scope. |

## Gate

All 24 calls must complete, preserve the complete target, cite authentic and relevant original lines for definitive decisions, satisfy these truth/conflict expectations, and leave the paused application hash-identical. Inspect rationales and quotations, not just status. Any critical miss is a failed candidate; preserve traces and report it. Passing permits only a bounded production verification, not an unattended full RCA/storyteller restart. Engineering tests alone never meet this semantic gate.

## Registered follow-up ablation

The first non-thinking five-case batch has already demonstrated false approval in saved cases 1 and 2. Complete and retain that batch; do not call it accepted or run all transfer cases needlessly on a rejected candidate. Before any thinking-enabled calls, register this one-variable comparison: repeat the same five claims, same documents, same exact prompt/schema, same digest/context/temperature, but explicitly set Ollama `think=true`. The installed model reports the thinking capability. No promise that this will improve the answer; the exact same semantic expectations apply. Persist the thinking option in request/cache identity and diagnostic manifest. If the five cases pass, run the unchanged remaining regression and transfer inputs in this mode. These are paired development observations, not a repeatability estimate.

## Registered contrastive-prompt candidate (v5.1)

The native-thinking comparison failed its first call at the unchanged 12,587-token allowance with no answer content. Its second call was explicitly interrupted; do not count it as a semantic verdict. No remaining thinking cases will run. Production default stays non-thinking.

Before new calls, register a prompt-only correction: add contrastive demonstrations about an event register/room attendance and an invoice excerpt/whole invoice. Include direct-observation and record-content positive controls so the model is not taught blanket rejection. No incident IDs, fixture text or answer keys are inserted. The complete-claim schema, evidence, Qwen identity and non-thinking settings remain the same as candidate A. Policy becomes `literal-claim-v5.1` to invalidate old review tasks/caches. The same 24 frozen semantic expectations above still apply. The first five are development regressions; the six new transfer inputs remain untested at registration time.

Observed caveat: changing prompt length changed context-planner selection and thus the source-bound grammar. Original input documents and claims were unchanged, but this was NOT a pure fixed-packet prompt ablation. Candidate C failed both original overclaims, plus invented a source conflict in case 2. Preserve that failure.

## Registered compact-contract candidate (v5.2)

Before its first live call: replace four duplicated response branches with a single shared shape and one relation enum. Preserve cross-field premise checks at parse/apply time. All outputs with original text must cite a real source/line; when there is no text, only not-established with empty evidence is permitted. Keep the v5.1 prompt and the non-thinking model configuration. Original documents and claims remain fixed, but retrieval can change as the smaller schema frees context. This is a combined contract/context-efficiency change, not a pure fixed-packet causal attribution. The 24 acceptance expectations remain unchanged. No new transfer cases have yet run.

## Registered sampling diagnostic — one saved claim only

The compact non-thinking candidate still falsely approves saved claim 1; saved claim 2 is now partial. Before the next call, register ONLY saved claim 1 with the same compact schema/prompt/evidence selection and model digest, native thinking enabled and the Qwen model-card Quickstart reasoning sampling profile: temperature 1, top_p .95, top_k 20, min_p 0, presence_penalty 1.5, repeat_penalty 1; seed 42 is an evaluator-added reproducibility setting. See https://huggingface.co/Qwen/Qwen3.5-9B . Its Quickstart and later Best Practices differ for non-thinking reasoning; this comparison explicitly uses the Quickstart thinking profile, not a claim of a uniquely optimal configuration. Context/output allowances stay fixed. This is a settings-profile comparison, not attribution to temperature alone. Expected partial/unknown, no conflict; no subsequent claims are queued by this one-case command. If it fails, do not silently increase limits, retry or change models.

## Registered prose feasibility probe

After the current one-case generation finishes, use `scripts/review-packet-unconstrained.mjs` on candidate D's saved case-1 request. Same model digest and exact original evidence packet; original non-thinking temperature-zero configuration and unchanged output allowance. Change to a short evidence-entailment task in ordinary prose, with no JSON schema. This tests whether the model can recognize the decisive distinction outside the application contract. Because both task prompt and output format differ, it does not isolate grammar as the cause. Expected: record listing/blank field established, actual attachment absence not established. It is a diagnostic only, never a production approval and never a substitute for the 24-case gate.

## Settings-profile extension

The one-case thinking/sampling comparison returned partial and preserved the decisive blank-field versus physical-absence distinction. Its positive-listing quotation is incomplete (the selected lines omit the ECP row), so do not call it perfect citation performance. The prose feasibility probe still falsely approved physical absence. Before any extension calls, keep the thinking/sampling profile and v5.2 prompt/schema frozen and test the unchanged seven regressions, six transfer regressions and six new transfer cases, followed by the remaining saved positive/overclaim controls. No automatic promotion to production; the full gate still applies. Report citation defects and classification errors even when a headline status matches.

## Registered non-thinking sampling ablation

After the current seven-case batch completes, test saved case 1 once with the identical v5.2 prompt/schema/evidence/model, the explicit sampling profile and seed 42, but `think=false`. This separates the mode toggle from the profile change. It also checks whether the improved decision is available without the multi-minute thinking cost. Expected partial/unknown with scope preserved; never silently adopt it from a single success. If it passes, any broader non-thinking qualification must be reported as a different profile, not combined with thinking-mode outcomes into one score. No hidden retries or seed search.

## Registered v5.4 correction: citation mapping and gap preservation

Before any new calls: keep the saved Qwen digest, review-only native thinking and sampling profile. Add exact-target wording to each original-line citation, so clause coverage is inspectable and supported claims cannot omit target wording. This structural check is not an entailment proof; independently inspect semantic relevance and complete-assertion coverage. Preserve the complete original target, with no separate interpreter or automatic verdict repair.

Move cross-field consistency validation to application time (still mandatory), allowing a structurally authentic inconsistent response's proposed missing premises to become evidence-request questions. Its claim remains proposed/review-incomplete, its task failed, and its semantic acceptance result failed. The question must retain provenance to that claim and must not assert its premise as a fact. Forged citations, malformed outputs and changed targets cannot supply recovery questions. Independent work can continue; causal verification cannot promote the failed claim.

Run the five original cases with `--runtime-defaults`, then the frozen seven regressions if their behavior warrants extension. The 24-case semantic gate is unchanged; question recovery is separately tested as failure containment and must not be scored as a correct verdict. A bounded, explicitly diagnostic board may still be examined from individually audited supported findings, but must not be represented as overall acceptance or an unattended investigation. The source investigation remains paused while these diagnostics execute. No new evidence or hidden answer is introduced.

## v5.4 rejected; v5.5 retains only failure-containment change

All five v5.4 calls finished. Case 1 falsely approved physical absence; case 2
still approved exhaustive absence semantically and failed its wording-map check;
case 3 had a correct positive judgment rejected by that same new map requirement.
Cases 4–5 passed. Do not extend this failed candidate or keep its added generation
requirement. Preserve the traces unchanged.

Before the board pilot: restore the v5.3 generation prompt/shape, retain only
application-time validation and proposed-gap recovery, and identify it as v5.5.
The three-finding board pilot uses freshly reviewed simple assertions from B0;
it is not a repeat of the failed compound claims or qualification of all cases.
Citation relevance/completeness remains an explicit audit—not a solved guarantee.
Replay the saved inconsistent v5.2 output offline to test failure containment;
that replay earns no new semantic pass and does not repair the original verdict.
